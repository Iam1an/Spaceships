[package]
name = "spaceships-protocol"
description = "Wire contract for the Spaceships WebSocket protocol (JSON, byte-compatible with the JS server/client)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
