//! hud — implementation pending.

use bevy::prelude::*;

/// Placeholder so `main.rs` can register this plugin before the module is
/// implemented. Replace the body; do not change the type name.
pub struct HudPlugin;

impl Plugin for HudPlugin {
    fn build(&self, _app: &mut App) {}
}
